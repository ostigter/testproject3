README
======

Tool to easily validate XML documents against their schema's.

Copy the schema's and documents anywhere in the project directory and run the
XmlValidator.bat script.

Uses Java's standard JAXP API and a map with the schema files mapped
by their target namespace.
